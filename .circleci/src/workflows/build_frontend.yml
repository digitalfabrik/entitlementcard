when: << pipeline.parameters.run_build_frontend >>
jobs:
  - check_frontend
  - build_android:
      matrix:
        parameters:
          buildConfig: [bayern, nuernberg, koblenz]
      requires:
        - check_frontend
  - build_ios:
      matrix:
        parameters:
          buildConfig: [bayern, nuernberg, koblenz]
      requires:
        - check_frontend