when:
  and:
    - << pipeline.parameters.run_commit >>
    - not:
        equal: [ main, << pipeline.git.branch >> ]
jobs:
  - check_backend
  - check_administration
# Commented out, because we test gh actions ./github/workflows/check-administration
#  - check_frontend
