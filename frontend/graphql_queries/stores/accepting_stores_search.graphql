query AcceptingStoresSearch($params: SearchParamsInput!) {
    searchAcceptingStores(params: $params) {
        id
        name
        description
        physicalStore {
            address {
                location
            }
            coordinates {
                lat,
                lng
            }
        }
        category {
            id
        }
    }
}
