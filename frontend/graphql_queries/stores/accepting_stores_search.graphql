query AcceptingStoresSearch($project: String!, $params: SearchParamsInput!) {
    stores: searchAcceptingStoresInProjectV2(project: $project, params: $params) {
        id
        name
        descriptions {
            locale
            text
        }
        physicalStore {
            id
            address {
                location
            }
            coordinates {
                lat,
                lng
            }
        }
        categoryId
    }
}
