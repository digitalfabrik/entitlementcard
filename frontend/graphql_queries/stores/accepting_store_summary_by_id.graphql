query AcceptingStoreSummaryById($ids: IdsParamsInput!) {
    physicalStoresById(params: $ids) {
        id,
        store {
            name
            description
            categoryId
        }
    }
}
