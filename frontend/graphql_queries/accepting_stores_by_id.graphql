query AcceptingStoreById($ids: ParamsInput!) {
    physicalStoresById(params: $ids) {
        store {
            id,
            name,
            description,
            contact {
                id,
                email,
                telephone,
                website
            }
            category {
                id,
                name
            }
        }
        address {
            street,
            houseNumber,
            postalCode,
            location,
            state
        }
    }
}
