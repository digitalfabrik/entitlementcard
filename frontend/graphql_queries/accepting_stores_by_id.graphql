query AcceptingStoreById($ids: ParamsInput!) {
    acceptingStoreById(params: $ids) {
        id
        name
        category { name }
        contact {
            email
            telephone
            website
        }
        phyiscalStore {
            address {
                coordinates {
                    latitude
                    longitude
                }
                houseNumber
                street
                postalCode
                location
                state
            }
        }
    }
}