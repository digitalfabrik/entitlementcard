library config_builder;

import 'package:build/build.dart';
import 'package:source_gen/source_gen.dart';

Builder configBuilder(BuilderOptions options) {
  throw 'wer';
  return SharedPartBuilder([], 'config_builder');
}
