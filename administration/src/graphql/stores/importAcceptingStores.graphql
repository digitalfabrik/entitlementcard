mutation importAcceptingStores($stores: [CSVAcceptingStoreInput!]!, $project: String!) {
  result: importAcceptingStores(stores: $stores, project: $project) {
    storesCreated
    storesDeleted
    storesUntouched
  }
}
