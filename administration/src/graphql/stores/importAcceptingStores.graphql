mutation importAcceptingStores($stores: [AcceptingStoreInput!]!, $dryRun: Boolean!) {
  result: importAcceptingStores(stores: $stores, dryRun: $dryRun) {
    storesCreated
    storesDeleted
    storesUntouched
  }
}
