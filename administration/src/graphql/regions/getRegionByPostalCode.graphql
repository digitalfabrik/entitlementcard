query getRegionByPostalCode($postalCode: String!, $project: String!) {
  region: regionByPostalCode(postalCode: $postalCode, project: $project) {
    id
    name
    prefix
    activatedForApplication
  }
}
