query getApplicationConfirmationNoteInformation($regionId: Int!) {
  region: regionByRegionId(regionId: $regionId) {
    applicationConfirmationMailNoteActivated
    applicationConfirmationMailNote
  }
}
