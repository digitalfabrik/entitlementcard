mutation verifyOrRejectApplicationVerification(
  $project: String!
  $accessKey: String!
  $verified: Boolean!
) {
  result: verifyOrRejectApplicationVerification(
    project: $project
    accessKey: $accessKey
    verified: $verified
  )
}
