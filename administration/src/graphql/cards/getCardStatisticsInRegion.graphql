query getCardStatisticsInRegion($regionId: Int!, $dateStart: String!, $dateEnd: String!) {
  result: getCardStatisticsInRegion(regionId: $regionId, dateStart: $dateStart, dateEnd: $dateEnd) {
    region
    cardsCreated
    cardsActivated
    cardsActivatedBlue
    cardsActivatedGolden
  }
}
