query getCardStatisticsInProject($dateStart: String!, $dateEnd: String!) {
  result: getCardStatisticsInProject(dateStart: $dateStart, dateEnd: $dateEnd) {
    region
    cardsCreated
    cardsActivated
    cardsActivatedBlue
    cardsActivatedGolden
  }
}
