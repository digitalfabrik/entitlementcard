mutation updateNotificationSettings($project: String!, $notificationSettings: NotificationSettingsInput!) {
  updateNotificationSettings(project: $project, notificationSettings: $notificationSettings)
}
