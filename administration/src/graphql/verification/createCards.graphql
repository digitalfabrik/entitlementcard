mutation createCards($project: String!, $cards: [CardCreationModelInput!]!) {
  cards: createCards(project: $project, cards: $cards) {
    dynamicActivationCodeBase64,
    staticVerificationCodeBase64
  }
}
