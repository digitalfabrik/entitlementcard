mutation sendApplicationAndCardDataToFreinet(
  $applicationId: Int!
  $project: String!
  $freinetCards: [FreinetCardInput!]!
) {
  sendApplicationAndCardDataToFreinet(applicationId: $applicationId, project: $project, freinetCards: $freinetCards)
}
