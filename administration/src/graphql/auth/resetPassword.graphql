mutation resetPassword($project: String!, $email: String!, $passwordResetHash: String!, $newPassword: String!) {
  result: resetPassword(
    project: $project
    email: $email
    passwordResetHash: $passwordResetHash
    newPassword: $newPassword
  )
}
